# E2E Smoke Checklist

- API starts and answers health request
- Login endpoint returns token for valid credentials
- Child can be created and fetched
- Event can be created and appears in timeline
- Telegram webhook endpoint accepts update payload
- Draft can be created and confirmed
- Daily and weekly analytics endpoints answer
- Export endpoint returns file metadata
